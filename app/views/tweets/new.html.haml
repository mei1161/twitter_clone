= form_with model: @tweet, local: true do |f|
  .form-group
    = f.label :content
    %br
    = f.text_area :content, rows: 10
  = f.fields_for :image do |ff|
    .form-group
      = ff.label :image
      %br
      = ff.file_field :image
  = f.submit '登録する'