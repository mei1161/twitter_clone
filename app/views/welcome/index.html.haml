%h1 Welcome#index

- if user_signed_in?
  = link_to "プロフィールを編集できるだなも",edit_user_registration_path
  = link_to "ログアウトするなだも",destroy_user_session_path, method: :delete
  %ul
    - @users.each do |user|
      %li= link_to "#{user.screen_name}のページ",user_path(user.screen_name)
    
  %ul
    - @tweets.reverse_each do |tweet|
      - if tweet.reply.present?
        - next
      - if tweet.type == "TweetRetweet"
        retweet
        - tweet = tweet.retweet.tweet
        - pp tweet
      = render partial: "tweet",locals: { tweet: tweet }

- else
  = link_to "ログインするなだも",user_session_path
  
  



