%h1 Welcome#index

- if user_signed_in?
  = link_to "プロフィルを編集できるだなも",edit_user_registration_path
  = link_to "ログアウトするなだも",destroy_user_session_path, method: :delete
  %ul
    - @users.each do |user|
      %li= link_to "#{user.screen_name}のページ",user_path(user.screen_name)
    
  %ul
    - @follow_users.each do |follow_user|
      - @user = User.find_by(id: follow_user)
      - @user.tweets.order(:created_at).reverse_each do |tweet|
        %li= tweet.content
- else
  = link_to "ログインするなだも",user_session_path
  
  



